import Button from "../button";
import TextInput from "../input";

const Search = ({}) => {
  return (
    <>
      <TextInput /> <Button textColor="red"> Search</Button>
    </>
  );
};
export default Search;
